# 中英文统一网站：设计与维护

## 当前架构

中英文站已统一为英文站的布局，不再使用两套独立主题。

- `static/css/design-tokens.css`：共用颜色与英文字体定义。
- `static/css/en-home.css`：两种语言共同使用的基础样式与首页布局。
- `static/css/en-site.css`：两种语言共同使用的内页组件、搜索筛选和原生弹窗。
- `static/css/zh-locale.css`：中文语言适配（中文字体、行距、换行），以及已确认保留的低饱和论文统计标签和紧凑活动行。不要在此复制一套布局。
- `static/js/en-home.js`：共用首页渲染器，依据 HTML 的 `lang` 加载 `data/en-home.json` 或 `data/zh-home.json`。
- `static/js/en-site.js`：共用内页渲染器，依据 `lang` 选择原有 JSON 的 `zh` / `en` 内容，搜索、主题、年份、类型筛选和图片／摘要弹窗共用一套逻辑。 中文研究方向卡片不显示 `summary.zh` 简短导语，首段详细正文放在标题下方、首张图示右侧；后续段落紧接首段，放在图片右侧同一文字列的研究脉络折叠区中，不再从下方整行左侧重新开始，也不重复首段。图片与文字顶部对齐，窄屏按图片、标题正文顺序纵向排列；无图片时正文使用整行。英文研究页现采用相同的图文、进展和折叠层级，英文原始正文保持完整，摘要用于手机端紧凑展示。
- `static/js/publication-stats.js`：中文论文页可选的期刊／会议统计。使用同一批论文数据，保留之前的匹配规则和累计数值；不会把筛选结果误当成累计统计。

旧 `main.css`、`styles.css`、`zh-site.css`、`site-renderer.js`、`zh-site.js` 保留为历史文件，但七个新版中文主页面不再加载它们，不再依赖 Bootstrap。

## 内容与生成

中文首页和内页已成为英文页面模板的中文版本：

- `data/zh-home.json`：中文首页简明介绍、研究卡片、精选论文与荣誉。`researchIntro` 保存三栏前的研究概述，`research[].descriptionHtml` 保存经确认的中文正文与站内链接；概述与卡片正文均使用常规字重，不添加加粗强调，保留标题层级及链接样式；页面生成和运行时渲染均使用这些字段，运行时经过共用富文本清理函数。中文卡片使用正文内链接，不再重复显示旧版底部跳转；英文卡片保留原有纯文本和底部链接。
- `data/zh-pages.json`：中文内页标题、介绍和原始书籍前言。
- `data/profile.json`：完整中文履历、教学经历、书籍资源、研究理念、联系方式与报告视频仍从这里读取。
- 其余研究、论文、芯片、活动和招聘数据继续保留在原有 JSON 中，不复制论文数据库。

运行 `npm run build:zh` 根据英文站的 header/footer 模板和中文数据生成七个中文 HTML 文件。生成器为 `scripts/build-zh-pages.js`；修改中文页面结构时优先修改生成器，不要只手动修改生成文件。

首页采用与英文站相同的左文右图、研究三列、精选论文列表、荣誉分类双栏、独立的教学／教材双栏及招聘区块。中文原有长文放入可展开区域；报告视频改为卡片式 B 站播放器，使用懒加载并关闭自动播放。书籍与课程资源、联系方式均保留。

中文首页开头完整展示经用户确认的三段简介：“个人身份与头衔 → 研究与项目 → 脑机接口技术转化”，不再展示教育经历段落。全文以 `data/profile.json` 的 `about.zh` 为唯一来源，由生成器预渲染，并由共用脚本更新到同一容器；不再重复展示旧版头衔行或把全文藏在折叠区。荣誉列表下方不再重复展示学术任职和联系电话；顶部简介中的任职信息及原始联系方式数据保留。简介正文统一使用常规字重与文字颜色，不对首段、头衔或成果数字加粗；仅保留姓名、标题层级与链接样式。中文导航字标不使用英文姓名后的装饰点，首屏排版在 `zh-locale.css` 中适配，英文文案与布局保持原样。

中文页面语言切换链接指向对应英文页面；正文链接也按当前语言解析。英文论文保留原始标题，不强行翻译论文名称。 精选论文的 `year` 用于匹配现有年度数据索引，`venueLabel` 用于展示期刊与正式卷期年份；在线首发与正式卷期跨年时，不要只改查询年份导致精选项失配。

## 研究脉络与进展速览

中文研究方向保留原有标题、图示和首段正文。标题之后、首段正文之前，展示最多两条有明确年份和论文链接的“进展速览”；完整研究脉络与相关论文收进首段之后、同一文字列内的折叠入口。首段与展开部分保持同样的正文字号，入口前间距 6px，按钮上下内边距 2px，展开正文上内边距 8px，避免阅读脱节。不用“最新成果”概括早期方向，避免把较旧的阶段性工作误称为近期突破。

- 数据入口是 `data/research.json` 中每个方向的 `progressHighlights`。按数组顺序展示，不自动按年份抓取或重排。每条包含 `year`、`textZh`、`paperLabel`、`href`，本轮另记录 `sourceCitation` 以追溯到原有正文中的依据；有论文库记录时保留 `paperTitle` 用于提示。
- 本轮提供 11 条候选节点，均依据原有论述整理；所有原有正文、引用顺序、图示及英文内容保持不变。部分方向只有一条，不为凑数添加成果。
- `year` 是明确的展示年份，不用于查询论文库。已收录论文使用站内 `publications.html?paper=...#publication-results` 定位；未收录的预印本沿用正文现有来源，并标明预印本。
- 后续加入新研究时，在完整论述中按逻辑衔接，再替换或补充需要前置的 1–2 项进展；不必为每篇新论文追加一整段。
- 中文折叠项改为“展开完整研究脉络与相关论文”，打开后显示“收起研究脉络”。英文保持原有布局与入口文案。

## 教学与教材

中文首页将荣誉与教学拆成两个独立、全宽的区块。“荣誉与奖励”内部左列展示个人荣誉，右列展示“论文与会议荣誉”，用于容纳论文／海报奖、亮点墙报与当期封面等不同类型的认可。右列数据来自 `data/zh-home.json` 的 `paperAwards`，按用户给出的年份倒序排列，同年保持录入顺序。默认显示最新四项，其余通过原生折叠项展开；所有条目静态生成并在运行时同步更新。每条主行显示会议／期刊与荣誉名称、年份，次行显示作者角色，均不加粗。长会议名可使用 `venueShort` 作简短展示，完整名称保留在链接或文字提示中。仅在用户提供明确链接或论文库中有可靠对应依据时添加链接，不猜测论文。已确认的条目使用 `publications.html?paper=IEEE文献编号#publication-results`，论文页读取该参数、预填对应论文标题并定位到筛选结果；清空搜索即可恢复完整列表。也支持 `paper` 参数使用论文库中精确的出版链接。`paperTitle` 保存已对应的完整论文名并显示在荣誉的悬停提示中。两条 2023 年 TBioCAS 荣誉已分别对应在线学习 SNN 处理器与事件驱动混合神经网络处理器，后一篇的共同第一作者角色在本地论文 PDF 中已确认；荣誉年份仍按用户提供的数据保留。机器学习与自然启发计算会议的名称与库内 MIND 2025 记录不一致，另有中国脑机智能大会墙报和 2026 半导体学报封面尚待对应，暂不加链接。“教学与教材”在下方另起一行，左列展示四条课程和共同授课说明，右列展示教材介绍及三张封面卡片。两组网格宽屏均为两列，760px 及以下上下排列，避免把短荣誉列表与整套教学内容绑在同一行。原有 #about 和 #awards 锚点继续保留。

- 课程条目来自 `data/profile.json` 的 `teaching.courses`，每条显示课程名、年份、学校及已有的本科／博士层次或主讲角色。共同授课老师及链接集中在 `teaching.collaborationZh`，避免逐条重复；均使用常规字重。
- 教材与著作介绍仍来自 `data/zh-home.json` 的 `teachingMaterials`，原来的中文授课长句不再显示；英文授课介绍保持不变。
- 三张卡片复用 `data/profile.json` 的 `teaching.books` 中现有封面与资源，`resourceTypeZh` 区分在线教材、英文专著、教改项目，`cardTitleZh` 提供短展示名。原始完整标题保留在链接提示和可访问名称中。
- 卡片桌面三列并排，520px 及以下两列，360px 及以下单列；封面使用 `object-fit: contain` 完整显示，不裁切。站内教材留在当前标签页，外部专著和项目介绍另开标签页。
- 中文专著卡片链接至现有 IEEE 出版介绍页，英文入口与英文文案保持原样。原有详细教学经历仍保留在资料文件中。

## 报告视频与近期活动

中文首页“报告与演示”分为上下两部分：

- **报告视频**：读取 `data/profile.json` 的 `demos` 数组，按数组顺序显示。每条使用 `bvid` 与 `titleZh`，可选 `researchId` 提供相关研究链接；`enabled: false` 可暂时隐藏条目。上传新视频后，把 BV 号及标题加入该数组即可，无需修改页面布局。视频保持在单行横向轨道中，桌面一屏最多三张卡片、601–980px 两张、600px 及以下一张。支持左右按钮、横向滚动以及轨道聚焦时的方向键与 Home/End；显示当前可见范围，没有更多内容时禁用相应按钮。增加视频不会继续向下堆叠；不生成占位视频，也不自动轮播。播放器懒加载、不自动播放，并始终保留“在 B 站观看”链接。
- **近期活动**：首页每次加载时独立读取 `data/coverage.json`，将有效活动日期标准化后倒序排列，显示最新 4 条（不足 4 条则按实际数量展示），仅展示日期和标题链接，不在首页加载图片；“活动记事”页面保留原有照片。外部报道链接有效时直接跳转原文；没有链接时跳转站内活动页。只需维护“活动记事”的数据，不复制首页活动记录，也不需要手动替换首页条目；已打开的页面需刷新以读取更新。
- 两部分独立加载；视频数据缺失不会隐藏活动。活动请求失败时显示提示和“活动记事”入口，空列表也有明确提示。英文首页内容不变。

## 芯片展示

中英文芯片页共用三列桌面网格：视口宽度大于 980px 时每行三张卡片，761–980px 时两列，760px 及以下单列。卡片内边距与图片展示高度适度压缩；参数说明采用 14px 字号、1.6 倍行距、4px 条目间距和 16px 底部间距，标题与论文链接字号保持不变。中文卡片的参数说明默认收进原生 `<details>`“技术参数”折叠项，按需展开；无参数时不显示空折叠项，切换分类后恢复收起。英文卡片仍直接显示参数。完整保留芯片参数数据、论文链接和点击放大功能。样式位于 `static/css/en-site.css`。

## 书籍阅读

五章教材继续采用 `static/css/zh-book.css` 的阅读模式，配色来自同一设计变量。长公式可在自身区域横向滚动。PDF 转换的旧参考文献页、旧书籍入口保持原样，以免破坏存档内容与引用。

## 检查

- `npm run check`：运行中英文静态回归检查、芯片渲染检查及首页媒体区检查。
- `npm run check:links`：检查荣誉到论文页的精确定位、站内相对链接、筛选清除、未命中与部分数据失败回退。
- `npm run check:awards`：检查论文荣誉排序、默认四项与折叠条目、作者角色、同名记录保留、链接安全及静态／动态渲染一致性。
- `npm run check:teaching`：检查课程条目、共同授课说明、教材介绍和三张资源卡片、链接目标、图片保留、常规字重及响应式样式。
- `npm run check:carousel`：检查视频左右切换、首尾边界、可见范围、窗口尺寸变化、键盘操作、减少动态效果偏好和事件清理；不替代浏览器视觉验收。
- `npm run check:media`：检查视频记录、卡片数量、懒加载、活动排序与最新四条选取、数据更新、空状态及失败回退；不替代浏览器中的 B 站播放验收。
- `npm run check:chips`：检查中文参数默认折叠、参数与论文链接保留、英文参数仍展开、分类筛选及空数据状态；不替代浏览器视觉验收。
- `node scripts/build-zh-pages.js --check`：确认中文生成页面与模板、数据一致，不修改文件。
- `scripts/check-zh-pages.js`：检查统一样式、移除旧依赖、导航语言、地标、唯一 ID、本地资源、精选论文、生成一致性和五章教材。
- `scripts/check-en-pages.js`：原有英文页面与资源、论文数据检查。

浏览器验收时同时检查中文和英文，因为渲染脚本是共用的。建议覆盖桌面／平板／手机、组合筛选、空结果、摘要与图片弹窗、移动菜单，以及中文首页简介、教学教材卡片、视频卡片及近期活动。

更新样式或脚本时同步更新模板中的缓存版本。仅本地构建与检查不会发布网站；部署仍须单独执行。

## 已选背景层次（2026-09-13）

用户对比曲线、浅方格和错位微点后，明确选择恢复浅方格版。使用两层 CSS 线性渐变绘制 48px 间距、1px 线宽的极淡灰蓝方格，恢复原方格版的浓淡与尺寸，不继续使用点阵。保留近白灰蓝底色、纯白内容区及轻微整体阴影；排版、字号、内容及英文站不变。原曲线 SVG 暂留但不再加载。

样式位于 `en-home.css` 末尾 `Quiet grid background` 区块，仅通过 `body.zh-unified` 作用于中文版。内页网格比首页更淡，760px 及以下隐藏网格并减弱内容阴影，打印时去掉纹理与阴影。无动画、背景图片请求或新增运行时脚本。中文生成器 `homeCss` 缓存版本为 12。仅修改本地文件，尚未发布。

## 英文首页姓名与学位（2026-09-13）

英文首页首屏改为同一基线的 `Jie Yang` 与较小字号的 `Ph.D.`，对应中文姓名与学位的排版。删除该区域原有的 `Westlake University · Hangzhou, China` 地点行与 `Researcher, Advanced Neural Chip Center` 职务行，简介、正文履历、页脚及结构化元数据保留。`hero-name` 仍只包含姓名，学位为相邻元素，避免共用渲染器更新姓名时覆盖学位。样式仅限定英文首页，中文不变；英文首页 CSS 缓存版本为 14。
## 中英文首页头像统一（2026-09-13）

以当前中文首页为准，英文头像保持同一图片来源，桌面及平板（761px 以上）宽 190px，手机（760px 及以下）宽 72px，高度按原图比例自适应；5px 小圆角、无边框、无描边、无阴影。移除英文头像下方独有的 `Circuits. Algorithms. Neural systems.` 图注。保留英文姓名学位与简介排版，不引入中文手机端隐藏姓名的规则。修改仅作用于英文首页，CSS 缓存版本为 15；中文保持原样。
## 英文 About me 简介

按用户最新确认，About me 仅保留 Research Professor 等现任职务、研究方向与技术转化这一条；移除博士教育和加拿大博士后经历条目。使用原生 `<ul>/<li>`、小圆点、悬挂缩进和 16px 条目间距，正文保持 16px 常规字重，不增加小标题或加粗。研究中心、Neuralicorn 和四个研究方向添加对应链接；研究方向使用 `research_en.html` 锚点，不写入本地预览地址。

唯一内容数据为 `data/en-home.json` 的 `backgroundItems`，英文 HTML 保留相同的单条静态内容，共用渲染器对英文列表通过已有 `safeRich` 清理后更新，中文继续走原有逻辑。英文首页缓存版本为 CSS 16、JS 17。荣誉、教学、首页简介及中文页面未改。静态数据一致性、链接与列表项检查已加入英文检查脚本，`npm run check` 全部通过；已在桌面和 390px 手机视口查看并验证无横向溢出。尚未发布。
## 英文 Selected honors

参考 `data/zh-home.json` 的个人荣誉，英文 Selected honors 同步四项：华瑙杰出青年奖（2026）、脑机接口青百荟青年专家（2025）、浙江省高层次人才培养计划青年人才（2025）、杭州市西湖明珠工程海外高层次人才（2022）。按用户要求排除两条九三学社荣誉，中文记录不变，不混入论文与会议荣誉。英文标题存于 `data/en-home.json` 的 `selectedHonors`，保留对应中文标题供悬停查看；英文静态 HTML 同步显示四项，与运行时数据一致。新增测试核对排除规则、条目对应关系、年份、顺序和静态内容；英文检查通过，桌面及 390px 手机预览无横向溢出。尚未发布。
## 英文荣誉与教学板块对齐中文版

英文首页保留已确认的 About me 文字与 `#about` 锚点，简介独立展示；荣誉与教学不再附属于旧的 About/Teaching 左右两栏。`#honors` 为独立全宽 Honors & awards，内部左列 Personal honors（保留四项、不含九三学社），右列 Paper & conference honors。论文荣誉依据中文八条记录翻译，保留年份、作者角色与已有论文链接，未知链接不补造；默认显示最新四条，其余通过英文原生折叠项展开。`#teaching` 为独立全宽 Teaching & textbooks，内部左列四项课程、年份、学校与合作授课说明，右列教材说明和三张封面卡片。手机改为单栏，教材卡片窄屏两列、360px 以下一列。标题字号、列间距、细分隔线、条目常规字重、封面 contain 展示参照中文组件。

`data/en-home.json` 新增英文 `paperAwards` 与 `teachingMaterials`；`data/profile.json` 仅补充已有课程和书籍的英文展示字段与合作说明，中文字段不变。英文专著卡片采用与中文版一致的已有 IEEE 出版页面，不跳往购物网站。共用论文荣誉与教学渲染器按文档语言选择字段及折叠文案，中文默认行为由原有测试持续验证。英文 HTML 保留完整静态回退，CSS/JS 缓存版本分别为 17/18。新增 `scripts/check-en-home-sections.js`，接入 `npm run check:en`，核对静态与运行时一致、年份与链接、英文字段、八条荣誉的折叠及响应式结构。尚未发布。

### 英文荣誉默认显示三项

考虑英文标题占用空间更大，英文 Paper & conference honors 默认显示前三项，将 ISCAS 2025 放入展开区域的首项；完整八项、年份、顺序、链接及作者角色全部保留。中文版仍默认显示四项。英文静态 HTML 和运行时使用一致的分组规则，英文 JS 缓存版本更新为 19。

英文论文荣誉的折叠入口简化为 `View more`，展开后仍为 `Show fewer`；默认三项与完整八项内容不变。英文 JS 缓存版本为 20。


## 英文简介整合学术身份

在当前英文首屏 `introHtml` 的现任职务之后、研究方向之前，加入 “I am also a Senior Member of IEEE and an Associate Editor of Neuroelectronics.”，期刊名称链接到已有编辑委员会页面。删除独立 `academic-roles` 行及对应的英文渲染调用，不再重复展示联合创始人或 BCI Industry Alliance 身份。共享 `profile.roles` 数据保留，中文不受影响。静态简介与数据保持一致；英文 JS 缓存版本为 21。


### 已确认的英文个人荣誉译名

英文 Personal honors 最终采用：Huanao BCI Award — Outstanding Young Scholar（2026）；BCI 100 Young Scholars（2025）；Young Talent, Zhejiang Provincial High-Level Talent Development Program（2025）；Overseas High-Level Talent, Xihu Pearl Program, Hangzhou（2022）。青百荟按用户解释表达“百名青年学者”，不增加 Top 排名含义。数据与静态 HTML 同步更新，年份、中文悬停标题、排版及九三学社排除规则不变。


## 英文 Research 版式与全站方格背景

英文 Research 采用中文版的分组文本导航（Current research / Earlier research），桌面图示与正文顶部对齐。每个当前研究方向显示同源论文记录的至多两条英文 Progress at a glance，随后在同一文字列显示原首段正文和后续研究脉络折叠项；移除重复的桌面短摘要，所有原英文正文段落、图片与论文链接保留。`data/research.json` 仅增加 `navigation.labelEn`、`mobileSummaryEn` 和进展 `textEn`，年份与论文来源不变。英文导航适当缩短标签，正文标题保持原样。

手机端采用折叠导航、左侧小图与右侧进展、下方完整研究脉络；首段正文在窄屏移入原折叠内容，返回桌面恢复原位，不复制正文。无进展的早期研究使用原英文摘要作简短介绍；大图、锚点、Escape 关闭导航和展开功能沿用共用处理器。中文版文案与布局保持不变。研究页英文样式位于 `en-site.css` 的独立限定区块；英文 Research CSS/JS 缓存版本为 12/32。

浅方格底纹在 `en-home.css` 中改为中英文共用的 `body:is(.zh-unified, [data-en-page])` 规则，保持原 48px 方格、浓淡、白色内容区与轻阴影。八个英文页面统一使用 `en-home.css?v=18`；内页减淡，手机与打印隐藏，和中文版一致。相关研究回归检查已更新以验证中英文共享结构、原文不丢失及英文控件；英文检查增加全站方格和缓存版本断言。尚未发布。

## 英文 Publications 对齐中文版

英文论文页采用中文版的标题与累计统计同排布局：总篇数、期刊／会议比例和分项计数，右侧为部分期刊与会议的分布图，桌面默认预览每组前两行，可通过 View more / View less 展开收起；手机端总数保持可见，分布图默认收起。标题保留稳定的 `pub-heading-copy` 包装，去掉原占据顶部整行的介绍文字。统计下方采用期刊／会议分类跳转、通信作者注释、年份导航和完整年度论文列表，所有论文记录、作者、摘要、PDF 与出版链接保留。

共用论文渲染器现为两种语言创建同一结构，通过 `t()` 本地化标题、标签及辅助文字。`publication-stats.js` 增加可选语言参数，默认中文以保持兼容；统计口径、匹配顺序和数据集不变。英文单篇论文链接仍显示累计总数，并能返回完整列表；可选统计脚本不可用时不阻止论文显示。英文页面引入统计脚本版本 6、共用页面脚本版本 33 和页面样式版本 13；原浅方格背景不变。增加英文布局、图表标签与计数一致性、手机展开收起和单篇累计数的回归检查。尚未发布。
### 英文 Research 页头排版试版

仅调整英文研究页头：导语最大行宽为 68ch，桌面自然分成两行；收紧页头顶部、标题与导语、导语与导航的距离，标题字号不变。Current research / Earlier research 位于各组链接上方，链接改为淡蓝色并以留白分隔，不再使用点分隔符；允许自然换行。Doctoral and postdoctoral work 移至 Earlier research 标题旁，用较小常规字重展示。中文仍维持原分组和说明位置，研究卡片、原文、图片及论文链接不变。手机保留折叠导航与 Escape 收起行为。英文研究页 CSS/JS 缓存版本为 14/34，已在桌面和 390px 视口检查。

### 英文 Research 导航主次布局试版

桌面端研究导航改为左侧约三分之二的 Current research 与右侧约三分之一的 Earlier research。五个当前方向按两列排布，两项早期方向纵向排列，并以淡竖线区分；博士及博士后说明放在早期研究标题下方。761px 以下仍采用上下排列和原折叠导航。导语改为 “Exploring neural interfaces, Mandarin decoding, and brain-inspired computing and vision.”，元描述同步。研究正文、方向顺序、链接及中文页面保持不变。英文研究页 CSS 缓存版本更新为 15。
